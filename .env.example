# AWS Configuration
AWS_ACCESS_KEY=
AWS_SECRET_KEY=
AWS_DEFAULT_REGION=
S3_BUCKET_NAME=
S3_FILE_KEY=
LOCAL_FILE_PATH=

# PostgreSQL Configuration (used by docker-compose, ingestion, dbt)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
DB_NAME=
DB_USER=
DB_PASSWORD=
# Use "db" when running inside Docker containers, "localhost" for local development
DB_HOST=db
DB_PORT=5432
